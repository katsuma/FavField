import Foundation

enum AppConstants {
    static var apiBaseURL: URL { AppConfig.apiBaseURL }
    static let appGroupID = "group.tv.katsuma.FavField"
    static let favoriteTeamKey = "favoriteTeamAbbr"
    static let widgetLoadingKey = "widgetLoading"
}
