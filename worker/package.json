{
  "name": "favfield-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250603.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.3",
    "wrangler": "^4.19.1"
  },
  "dependencies": {
    "cheerio": "^1.0.0"
  }
}
